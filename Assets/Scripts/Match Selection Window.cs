using UnityEngine;

public class MatchSelectionWindow : MonoBehaviour
{

    void Start()
    {
        gameObject.SetActive(true);
    }

    //the match that is selected needs to send details to opponent script, but thats on the buttons, not this.

    //make top match active

    //unlocking / completing match.

}
